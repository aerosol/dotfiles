defmodule ${1:app}.${2:Module}Test do
  use $1.ModelCase

  alias $1.$2

  @valid_attrs %{$0}
  @invalid_attrs %{}

  test "changeset with valid attributes" do
    changeset = $2.changeset(%$2{}, @valid_attrs)
    assert changeset.valid?
  end

  test "changeset with invalid attributes" do
    changeset = $2.changeset(%$2{}, @invalid_attrs)
    refute changeset.valid?
  end
end
