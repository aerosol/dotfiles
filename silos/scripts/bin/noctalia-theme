#!/bin/sh

NOCTALIA_DARK=$(jq -r '.colorSchemes.darkMode' ~/.config/noctalia/settings.json)

if [ "$NOCTALIA_DARK" = "true" ]; then
	INIT=1
else
	INIT=2
fi

cat > ~/.config/foot/theme.ini <<EOF
initial-color-theme=$INIT
EOF
