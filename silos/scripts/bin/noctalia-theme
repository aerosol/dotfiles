#!/bin/bash
DARK_MODE=$(jq -r '.colorSchemes.darkMode' ~/.config/noctalia/settings.json)

if [ "$DARK_MODE" = "true" ]; then
	INIT=1
else
	INIT=2
fi

cat > ~/.config/foot/theme.ini <<EOF
initial-color-theme=$INIT
EOF

pkill -USR${INIT} foot
