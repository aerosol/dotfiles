#!/usr/bin/env bash
TERMINAL=alacritty
WID=$(xdotool search $TERMINAL 2>/dev/null | sort | head -1)
if [ -z "$WID" ]
then 
  echo "running"
    $TERMINAL
else 
  echo "wid $WID"
    xdotool windowactivate --sync $WID;
fi
