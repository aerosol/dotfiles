#!/usr/bin/env bash
notify-send -u low "Editing clipboard..."
f=~/.cache/clipboard
wl-paste > "$f"
footclient nvim +startinsert "$f" && wl-copy < "$f"
notify-send -a "nvim" -u low "Clipboard edited: $(wl-paste -t text)"
